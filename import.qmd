---
title: "Titre de votre page d'importation"
title-block-banner: true
description: | 
  Petite description de cette page.
# Importation de données
date: "2022-12-20"
# Modifier les détails que vous voulez
author:
  - name: "Abdoul Dramane TONDE"
    # Votre site web perso ou github
    url: https://github.com/Dramane98
    # les champs d'affiliation sont optionnels, vous pouvez les
    # comment out en ajoutant un # devant.
    affiliation: FAS1002
    affiliation-url: https://FAS1002.github.io/A22
    # changer pour votre propre orcid id
    # https://orcid.org/ pour vous inscrire.
    orcid: 0000-0000-0000-0000

# TRUE == Générer une citation pour cette page précise. Pour enlever, mettre false.
citation: true
# Inclure les références que vous utilisez dans vos rapports. Je conseille Zotero pour construire
# ce fichier ou de connecter RStudio directement pour pouvoir citer avec @nom-de-reference.
bibliography: references.bib
---

```{r echo=FALSE, warning=FALSE, message=FALSE}

library(tibble)
library(fs)
library(lubridate)
library(tidyverse)
library(skimr)
library(magrittr)
library(rvest)
library(readr)
```

```{r download, cache=TRUE}

owid_Data <- "https://ourworldindata.org/"

scaffold.owid<- paste0("data/raw/","owid-co2-data_", today(), '.csv')
#Le ! a été ajouté pour signifié NOT comme dans:
# Si (le fichier n'existe PAS) {
#    Exécuter cette parti de code
#    } sinon {
#    Exécuter cette partie de code
#    }
if (!file.exists(scaffold.owid)) {
  download.file(url = owid_Data,
                destfile = scaffold.owid) 
  paste("Le fichier",
        scaffold.owid,
        "n'existait pas, il a donc été téléchargé à l'instant."
  )
  
} else {
  print(paste("Le fichier",
              scaffold.owid,
              "existe déjà, il ne sera pas téléchargé à nouveau.")
  )
}




```

```{r}


leab_Data <- "https://www.gapminder.org/data/documentation/gd004/"

scaffold.Leab<- paste0('data/raw/',"leab-data_", Sys.Date(), '.csv')
#Le ! a été ajouté pour signifié NOT comme dans:
# Si (le fichier n'existe PAS) {
#    Exécuter cette parti de code
#    } sinon {
#    Exécuter cette partie de code
#    }
if (!file.exists(scaffold.Leab)) {
  download.file(url = leab_Data,
                destfile = scaffold.Leab) 
  paste("Le fichier",
        scaffold.Leab,
        "n'existait pas, il a donc été téléchargé à l'instant."
  )
  
} else {
  print(paste("Le fichier",
              scaffold.Leab,
              "existe déjà, il ne sera pas téléchargé à nouveau.")
  )
}

```
